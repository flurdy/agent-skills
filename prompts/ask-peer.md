---
description: Ask an independent peer for a repository-grounded second opinion
argument-hint: "<question>"
---
Use the `second-opinion` skill in `ask` mode with `--agent peer` to answer this question:

$ARGUMENTS

Follow the skill's independence-selection, context-sanitization, read-only, cost-consent, and failure-reporting rules.
