---
description: Ask Codex Sol for a repository-grounded second opinion
argument-hint: "<question>"
---
Use the `second-opinion` skill in `ask` mode with `--agent codex --model gpt-5.6-sol` to answer this question:

$ARGUMENTS

Follow the skill's context-sanitization, read-only, and failure-reporting rules.
