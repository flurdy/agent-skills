---
description: Ask Claude Fable for a repository-grounded second opinion
argument-hint: "<question>"
---
Use the `second-opinion` skill in `ask` mode with `--agent claude --model fable` to answer this question:

$ARGUMENTS

Follow the skill's context-sanitization, read-only, and failure-reporting rules.
