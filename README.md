# Shared

Shared skills used across clients and machines.

## Layout
- `skills/`: skill folders containing `SKILL.md` plus optional assets, scripts, and references

To add a new shared skill, create a new folder under `skills/` and include a `SKILL.md`.
